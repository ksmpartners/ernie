Put ernie-api fat jar and BIRT 4.2.2 Runtime jars in lib directory.

Place a report design, new_report.rptdesign, in this directory (at the same level as this file).

To execute the test:

```
    sbt
    clean
    run
```